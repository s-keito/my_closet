  <div class="container">
   <div class="row my-5">
    <div class="col"></div>
     <div class="col">
       <div class="dress_show">
         <div class="dress_image"><%= image_tag @dress.get_image, size: '400x400' %></div>
          Posted date：<%= @dress.created_at.strftime('%Y/%m/%d') %><br>
           Caption:：<%= @dress.caption %><br>
           Category：<%= @dress.category %><br>
           Season：<%= @dress.season.name %><br>
           <!--公開：</%= @dress.is_status %><br>-->
          <a><%= link_to user_path(@dress.user), style:"font-size:18px;" do %>
         <%= @dress.user.name %><% end %></a>
        </div>

        <div class=<%= "favorite-btn-#{@dress.id}" %> >
         <%= render 'favorites/favorite-btn', dress: @dress %>
        </div>

        <div class="btn">
        <% if @dress.user == current_user %>
         <%= link_to edit_dress_path(@dress) do %>
          <button type="button" class="btn btn-secondary">edit</button>
         <% end %>
         <%= link_to dress_path(@dress), method: :delete,"data-confirm" => "本当に削除しますか？" do %>
          <button type="button" class="btn btn-secondary">delete</button>
         <% end %>
        <% end %>
       </div>

       <div class="comment_count my-4">
        <%= render "comments/counter", dress: @dress %>
       </div>

       <div class=<%= "comment_form-#{@dress.id}-#{@comment.id}" %> >
         <%= render 'comments/form', dress: @dress, comment: @comment %>
   　   </div>

       <div class="comments_index my-5">
        <%= render "comments/index", dress: @dress %>
       </div>
     </div>
    <div class="col"></div>
  </div>
</div>
