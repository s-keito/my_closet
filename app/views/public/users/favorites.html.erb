<div class="container">
  <div class="row">
    <div class="col-1"></div>
     <div class="col-10 my-5">
      <h4 class="text-center">いいね一覧</h4>
      <% @favorite_dresses.each do |dress|%>
      　<%= link_to dress_path(dress) do %>
          <%= image_tag dress.get_image, size:'200x200' %>
        <% end %>
      <% end %>
     </div>
    <div class="col-1"></div>
  </div>
</div>