  <div class="btn">
    <button type="button" class="btn btn-secondary">
      <% if current_user.following?(user) %>
        <%= link_to "フォロー中", user_relationships_path(user), class:"btn btn-secondary", method: :delete, remote: true %>
      <% else %>
        <%= link_to "フォローする", userrelationships_path(user), class:"btn btn-secondary", method: :post, remote: true %>
      <% end %>
    </button>
  </div>
